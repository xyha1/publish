# publish

Initial Java migration of postiz-app (iterative). Contains MySQL schema and Spring Boot + MyBatis project skeleton implementing core Facebook integration (OAuth, list pages, fetch page token, post to page) and token refresh flow. Java 8 / Maven / package: easysc.